//! Only a short fix i wanna add a complex number class a little later but for now this will do

/**
 * Represents a complex number with real and imaginary components.
 */
export type ComplexNumber = {
    real: number;
    imaginary: number;
};

